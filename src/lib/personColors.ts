// Fixed You/Partner pair for comparison bars (Log screen's sticky summary,
// Dashboard's "Who paid" card) — spaces cap at 2 members, so a role-based
// pair is enough; no per-user hash needed. Same muted-chart-color register
// as categoryColors.ts, deliberately distinct from every category hue so
// the two palettes never coincide across screens.
export const YOU_COLOR = '#8ec98a'
export const PARTNER_COLOR = '#d992ae'
